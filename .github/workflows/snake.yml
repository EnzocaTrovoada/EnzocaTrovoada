name: snake animation

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#e0e0e0&color_dots=#161b22,#3a3a3a,#6b6b6b,#a0a0a0,#e0e0e0
            dist/github-contribution-grid-snake.svg?palette=github-light&color_snake=#0a0a0a&color_dots=#ebedf0,#c0c0c0,#888888,#555555,#1a1a1a
      - uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
